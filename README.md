# Frontend Mobile Calendar - Тестовое задание

Этот проект представляет собой реализацию кастомного мобильного календаря для выбора диапазона дат, выполненного в рамках тестового задания. Основной упор был сделан на создание плавного, интуитивного и "нативного" пользовательского опыта, а также на чистую и масштабируемую архитектуру кода.

## 🚀 Демо

[Посмотреть демо](https://calendar-bu8a0x2xg-tatyanas-projects-efeeb17b.vercel.app)

## 🎯 Ключевые решения и их обоснование

### 1. Архитектура: Feature-Sliced Design (FSD)

Я выбрал FSD для организации кодовой базы. Эта методология обеспечивает строгие правила импорта между слоями (`shared`, `entities`, `features`, `widgets`, `pages`, `app`), что делает проект легко масштабируемым, поддерживаемым и тестируемым. Каждый слой имеет четкую зону ответственности, что предотвращает "спагетти-код".

### 2. Пользовательский опыт (UX)

- **Вертикальный скролл по месяцам**: Вместо стандартных переключателей "вперед/назад", я реализовал бесконечный вертикальный скролл. Это создает ощущение единой ленты, как в популярных социальных сетях (Instagram, TikTok), что соответствует требованию "Tinder/Instagram feel".
- **CSS Scroll Snap**: Для четкой и предсказуемой навигации между месяцами используется `scroll-snap`. При скролле страница автоматически "прилипает" к ближайшему месяцу, что делает интерфейс отзывчивым и удобным для мобильных устройств.
- **Бесконечная прокрутка (Infinite Scroll)**: Новые месяцы подгружаются динамически по мере прокрутки вверх или вниз с помощью `IntersectionObserver`. Это обеспечивает высокую производительность, так как в DOM одновременно находится ограниченное количество месяцев.
- **Начальная позиция**: При загрузке календарь автоматически прокручивается к текущему месяцу, чтобы пользователь сразу видел актуальные даты.
- **Индикация выбранного диапазона**: В нижней части экрана находится фиксированная панель, которая всегда показывает выбранный начальный и конечный день.

### 3. Решение проблемы "Клик vs. Скролл"

Проблема пересечения кликов и скролла решена наиболее нативным и простым способом:
- Для скролла используется стандартное поведение браузера, управляемое CSS `overflow-y: auto` и `scroll-snap`.
- Обработчики кликов (`onClick`) навешаны на элементы `<button>` внутри дней.
Браузеры на мобильных устройствах достаточно умны, чтобы отличить жест скролла/свайпа от тапа/клика. Во время скролла событие `click` не вызывается, что предотвращает случайный выбор даты при прокрутке. Это решение не требует сложной логики с `onTouchStart`/`onTouchMove`/`onTouchEnd` и является самым надежным.

### 4. Стек технологий

- **Next.js / React**: Современный и производительный фреймворк для создания React-приложений.
- **TypeScript**: Для строгой типизации и улучшения опыта разработки.
- **Tailwind CSS**: Для быстрой и консистентной стилизации без написания кастомного CSS. Идеально подходит для создания красивого интерфейса без готового дизайна.
- **date-fns**: Легковесная и модульная библиотека для работы с датами.
- **clsx**: Утилита для условного составления CSS-классов.

## ⚙️ Как запустить локально

1.  Клонируйте репозиторий:
    ```bash
    git clone <URL репозитория>
    ```
2.  Установите зависимости:
    ```bash
    npm install
    ```
3.  Запустите dev-сервер:
    ```bash
    npm run dev
    ```
4.  Откройте [http://localhost:3000](http://localhost:3000) в вашем браузере.
